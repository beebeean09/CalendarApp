import React from 'react';

class CalendarEvents extends React.Component{
  render() {
      return (
        <div>
         // render events from calendar after retrieving from database
        </div>
      );
  }
}

module.exports = CalendarEvents;
